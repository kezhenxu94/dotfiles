# Unix
alias ll="ls -al"
alias ln="ln -v"
alias svn="docker run -it --rm -v \$PWD:/workspace -w /workspace python:3 svn"

alias k='kubectl'
alias n='nvim'

# Include custom aliases
if [[ -f ~/.aliases.local ]]; then
  source ~/.aliases.local
fi
