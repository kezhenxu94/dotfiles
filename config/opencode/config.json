{
  "$schema": "https://opencode.ai/config.json",
  "theme": "catppuccin",
  "autoupdate": true,
  "command": {
    "commit": {
      "template": "Create a conventional commit for the current changes. First, stage any unstaged changes, then create a commit with a proper conventional commit message following the conventional commits specification.",
      "description": "create a git commit based on current changes"
    }
  }
}
